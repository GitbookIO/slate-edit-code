
nodes:
  - kind: block
    type: code_block
    nodes:
      - kind: block
        type: code_line
        nodes:
          # Contains another block instead of just text nodes
          - kind: block
            type: default
            nodes:
              - kind: text
                text: "Some\n code"
